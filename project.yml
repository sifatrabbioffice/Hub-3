name: GameHub
options:
  bundleIdPrefix: com.sifatrabbioffice
targets:
  GameHub:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - GameHub
    settings:
      # Fixes the SwiftUICore linking warning
      ENABLE_USER_SCRIPT_SANDBOXING: NO
      SWIFT_OPTIMIZATION_LEVEL: "-O"
